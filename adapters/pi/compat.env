# Pi version compatibility declaration (consumed by pi-code and CI).
# Minimum: Pi 0.79.0 introduced extension-controlled project trust
# (project_trust event, --approve/--no-approve) — required by FR-004a.
CCT_PI_MIN_VERSION="0.79.0"
