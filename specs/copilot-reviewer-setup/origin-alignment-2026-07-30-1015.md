# Origin alignment check — copilot-reviewer-setup

Origin: verbatim user directive of 2026-07-30 (quoted in plan.md `origin:`),
including the full AGENTS.md/CODE_REVIEW.md design supplied in the request.
The two `urls:` entries are the vendor documentation the design cites
(AGENTS.md discovery, Codex GitHub review); they are supplementary to the
in-conversation directive, which is the primary origin and was read in full.

Origin claim:
> Provide a script — Codex first, but abstracted from the beginning — that
> sets up a copilot as an independent code reviewer for a project. Use a
> root AGENTS.md loader section plus a committed docs/CODE_REVIEW.md holding
> the full review workflow. If the AGENTS.md is generated by
> code-copilot-team, the rules must ship at the generator layer so
> regeneration does not erase them.

Working claim:
> spec.md delivers an abstracted per-copilot installer
> (`setup-reviewer.sh --codex`, dispatch table for future tools) installing
> the committed review workflow (`docs/CODE_REVIEW.md`, managed/refreshable),
> a project-owned configuration file (`docs/CODE_REVIEW_PROJECT.md`, never
> overwritten once customized), and a marker-guarded loader block in the
> project root `AGENTS.md`; generated instruction files receive the same
> block at the generator layer (`generate.sh` → `adapters/codex/AGENTS.md`)
> so regeneration preserves it, and a stale generated file fails the install
> loudly instead of reporting an inert success.

Mismatches:
  - none. The project-owned configuration split (added after the
    independent review of PR #148) refines the origin's single-document
    layout to satisfy its own instruction that users fill in placeholders —
    the origin's placeholder-editing step is impossible to honor safely
    with a fully managed single file.

Verdict: aligned
Confidence: medium

Note: medium, not high, because the two cited vendor URLs were not
re-fetched this session; the primary origin (the verbatim user directive
and inline design) was read in full.
