# verification.yaml — requirement→verifier evidence graph (#190 §3).
# Generated by scripts/generate-verification-draft.sh — review each
# entry, replace every placeholder with a real executable verifier,
# then set status: finalized. spec.md is the only authoritative
# source of requirement text; statement below is display-only and
# statement_sha binds it (admission recomputes and fails on drift).
# Verifier kinds: deterministic (test: <path|command>),
#   runtime_conformance (criterion: judged against the RUNNING app),
#   visual (criterion: judged against the RENDERED app). Mapping any
#   FR to kind: visual is what puts UI IN SCOPE — admission then
#   requires the UI bundle (DESIGN.md, harness/, copilot:review).
# The visual placeholder below each FR is an author-decision
#   SCAFFOLD, not a claim that the FR is UI-visible: write a
#   concrete criterion if it is, otherwise REMOVE the verifier.
#   Leaving it unresolved keeps the artifact inadmissible.
# Contract: shared/schemas/verification.schema.json
status: finalized
feature_id: runs-attempt-history

FR-1:
  statement: "The run record gains `probe`: from `reviewer-probe.json`, `{provider, requested_provider, verdict, parseable, duration_sec, invocation_cost_usd, error}`; `null` when the file is absent or unreadable."
  statement_sha: "sha256:74e005a4c5a61def4101f9378c05a901dbddf42a4676a85f32c60a7ffa0ca712"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr1"
FR-2:
  statement: "The run record gains `earlier_terminations`: one entry per `termination-<epoch>.json` in the ledger directory — `{reason, detail, phase, created, file}` — sorted by `created` ascending; an empty list when there are none. The current `disposition` is unchanged (it still comes from `termination.json` or the newest escalation)."
  statement_sha: "sha256:c510ef0481b38b208f06d6f399bca2ff705768d76af8be76f3a0607293855ede"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr2"
FR-3:
  statement: "The run record gains `fallbacks`: one entry per phase whose newest round's findings file carries `fallback` — `{phase, round, from, error, to}` where `to` is that round's `reviewer_provider`; the findings file is `phase-N/review/findings-round-<round>.json` for the highest round present. An empty list when none."
  statement_sha: "sha256:0e7ab2df5a606808fa6dc33ac779642d22f58cc865d9ef69d6bba1f83fe5fd4c"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr3"
FR-4:
  statement: "`render_run` prints a probe line (\"probe: <provider> answered <verdict> in <n>s, <cost or unmetered>\" or \"probe: none recorded\"), one line per earlier termination, and one line per fallback; `render_list` appends \"resumed after N termination(s)\" to a run's line when `earlier_terminations` is non-empty."
  statement_sha: "sha256:1b9010b6b0caab9d5c6f984f7b77edcd4f7d00b8a206ac901a57ebdd027bd920"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr4"
FR-5:
  statement: "`studio/lib/runsView.ts` gains pure `probeLine(run)`, `earlierTerminationsLine(run)` (\"landed after 1 earlier termination (provider_unavailable)\" / \"\" when none) and `fallbackLines(run)`; `app/runs/page.tsx` renders them on the card; `studio/lib/api.ts` declares the three fields; `studio/scripts/states-check.mjs` asserts the three helpers on a run with and without each fact."
  statement_sha: "sha256:58b249c2042335649a5909c9fe37b98af5ab7e4c97a93b711d16e62cb8e7dd98"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr5"
FR-6:
  statement: "Tests: `tests/test_auto_build_runs.py` builds ledgers with and without each artifact and asserts FR-1..FR-4, including an unreadable probe file and a findings file without `fallback`; every test function's name carries `hist_fr<N>` for the requirement it verifies. FR-5's states-script assertions are verified by the PR's CI `studio` job (admission and the verifier gate run in a throwaway worktree without `studio/node_modules`, so `npm run states-check` cannot be a verifier on this host); its deterministic verifier is a Python test that the three field names the helpers read are present on the record. The cookbook's Runs section names the three facts."
  statement_sha: "sha256:6ab18b273546a74f6f2bb8653e760eec0c9fbd36ca87009a18b8312767a5aa95"
  verifiers:
    - kind: deterministic
      test: "env PYTHONPATH=scripts:. python3 -m pytest scripts/session_analytics/tests/test_auto_build_runs.py -q -p no:cacheprovider -k hist_fr"
