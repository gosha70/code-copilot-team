Run the full build pipeline for all modules. Verify annotation processor output, run compile-testing tests, validate generated code compiles, and check integration tests pass. Report any processor compilation errors with full diagnostics.

```bash
./gradlew clean build
```
