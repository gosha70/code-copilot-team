Perform a full team review of recent changes:
1. As QA: run all tests (unit, component, e2e), report coverage and failures
2. As QA: report runtime observability statuses: `ui-smoke`, `console`, `network`, `visual` (PASS/FAIL/SKIP + evidence)
3. As Backend Developer: review API routes, auth checks, DB queries
4. As Frontend Developer: check responsive behavior, component accessibility, and browser console cleanliness
5. As DevOps: verify build succeeds, check bundle size, preview deployment, and failed network calls
6. As Team Lead: synthesize into prioritized action items
