{
  "patterns": [
    {
      "match": "PrismaClientInitializationError",
      "hint": "Prisma client not generated or DB unreachable. Run: npx prisma generate && npx prisma migrate dev"
    },
    {
      "match": "NEXT_PUBLIC_",
      "hint": "Client-side env vars must be prefixed with NEXT_PUBLIC_. Server-only vars should NOT have this prefix"
    },
    {
      "match": "relation .* does not exist",
      "hint": "Database schema out of date. Run: npx prisma migrate dev (or npx prisma db push for prototyping)"
    },
    {
      "match": "Module not found.*@prisma/client",
      "hint": "Run: npx prisma generate (regenerates the Prisma client after schema changes)"
    },
    {
      "match": "NEXTAUTH_SECRET",
      "hint": "NEXTAUTH_SECRET not set. Generate one: openssl rand -base64 32 (add to .env.local)"
    },
    {
      "match": "use client.*fetch|fetch.*use client",
      "hint": "Data fetching in Client Component. Must be in Server Components or Route Handlers only. See PROJECT.md #Architecture Rules."
    },
    {
      "match": "process\\.env[^.]|process\\.env\\.",
      "hint": "Raw process.env access. Validate all env vars with zod at startup. See PROJECT.md #Architecture Rules."
    },
    {
      "match": "no-explicit-any|Type.*any.*not assignable",
      "hint": "TypeScript strict: no 'any' types. Define proper interfaces. See PROJECT.md #Architecture Rules."
    }
  ]
}
