{
  "name": "code-copilot-team-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Starlight documentation site — content is staged from the repository, never stored here.",
  "scripts": {
    "content": "node build-content.mjs",
    "dev": "npm run content && astro dev",
    "build": "npm run content && astro build",
    "preview": "astro preview",
    "check": "npm run content && astro build && node check-site.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "0.42.1",
    "astro": "7.3.2"
  }
}
